{
  "private": true,
  "scripts": {
    "setup": "cd apps/web && npm install && npm run dev:setup",
    "services:up": "docker compose -f docker/docker-compose.yml up -d",
    "services:stop": "docker compose -f docker/docker-compose.yml stop",
    "services:down": "docker compose -f docker/docker-compose.yml down"
  }
}
